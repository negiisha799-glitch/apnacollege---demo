#include <stdio.h>
int main() {
  

    for(int i=1; i<=100; i++) {
        if(i % 2 != 0) {
            continue; // Skip even numbers
        }
        printf("%d ", i); // Print only odd numbers
    }
    return 0;
}
